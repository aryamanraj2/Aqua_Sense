// Secrets.xcconfig.example
// Copy this file to Secrets.xcconfig (already in .gitignore) and fill in real values.
// In Xcode: Project → Info → Configurations → set all configs to use Secrets.xcconfig

GEMINI_API_KEY = your-gemini-api-key-here
